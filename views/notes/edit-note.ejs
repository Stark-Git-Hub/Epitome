<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head.ejs') %>
<body>
    <%- include('../partials/nav.ejs') %>
    <div id="main">
        <div id = "ce-note" class="note">
            <h2 style="text-align: center;">Edit Note</h2>
            <p style="font-size: 15px; margin-top: -15px;"><%= note.category %></p><hr>
            <form action="/notes/edit-post/<%=note._id%>" method="POST">
                <label for="note-title">Title</label>
                <input type="text" id="note-title" name="title" value="<%= note.title%>" required>
                <label for="body">Content</label>
                <textarea id="content" name="content" required><%= note.content%></textarea>
                <button type="submit"">Save</button>
            </form>
        </div>
    </div>
</body>
</html>

